# Dependencies required for running the "stable" version of alphaviz.
# Only usage of fixed versions is allowed, and all dependencies listed here must also be
# included in `requirements_loose.txt` (enforced by a test).
# Pins seeded from requirements.freeze.txt (alphaviz 1.3.0, Python 3.9).
alphatims==1.1.0 # test: tolerate_version
peptdeep==1.5.0 # test: tolerate_version
alphabase==1.9.0 # test: tolerate_version
alpharaw==0.6.0 # test: tolerate_version

xarray==2023.12.0
multipledispatch==1.0.0
plotly==5.8.0
